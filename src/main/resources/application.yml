server:
  port: 8080

spring:
  application:
    name: placementgo-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/placementgo
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true

jd-intel:
  search-api-key: ${SEARCH_API_KEY:your-serp-api-key-here}
  search-engine-id: ${SEARCH_ENGINE_ID:your-search-engine-id-here}

gemini:
  api-key: AIzaSyDNmiJip-8i1lN-bqDRiZFBsOcdjoaIR9U
  base-url: https://generativelanguage.googleapis.com/v1beta
  model: gemini-2.5-flash
  temperature: 0.2
